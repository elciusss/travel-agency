name: pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update && sudo apt-get install firefox xvfb -y
      
      
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: export MOZ_DISABLE_OOP_PLUGINS=1
          export MOZ_DISABLE_GPU=1
          xvfb-run --auto-servernum firefox --headless --no-remote --disable-gpu --disable-gl-drawing --disable-gl-renderer --disable-webgl index.html

  deploy-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run : echo "finalizado"
  
    
  
